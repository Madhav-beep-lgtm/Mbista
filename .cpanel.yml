---
deployment:
  tasks:
    # All deployment steps live in deploy/tasks.sh (shared with the cron
    # auto-deploy). It detects the correct document root automatically.
    - /bin/bash deploy/tasks.sh
